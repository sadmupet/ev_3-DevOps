# NOTA: este archivo es una referencia para despliegue MANUAL/local (deploy.sh).
# En el pipeline de GitHub Actions este Secret NO se aplica desde aquí: se genera
# dinámicamente en el job "deploy" a partir del GitHub Secret DB_ROOT_PASSWORD,
# para no dejar la contraseña real hardcodeada en el repositorio.
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: tienda
type: Opaque
data:
  MYSQL_ROOT_PASSWORD: YWRtaW4xMjM=
